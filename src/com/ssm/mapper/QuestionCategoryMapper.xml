<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssm.dao.TQuestionCategoryMapper">
	<resultMap type="com.ssm.pojo.TQuestionCategory" id="TQuestionCategoryMap">
		<id property="id" column="id" />
		<result property="categoryName" column="category_name"/>
		<result property="parentId" column="parent_id"/>
		<result property="categoryPicture" column="category_picture"/>
		<result property="createPerson" column="create_person"/>
		<result property="createTime" column="create_time"/>
		<result property="updateTime" column="update_time"/>
		<result property="status" column="status"/>
	</resultMap>
	<select id="getProblemSetList" resultMap="TQuestionCategoryMap">
		SELECT * FROM t_question_category WHERE parent_id = #{parentId} AND `status` = 1 
		LIMIT #{page},#{limit}
	</select>
	
	<select id="problemSetCount" resultType="int">
		SELECT COUNT(0) FROM t_question_category WHERE parent_id = 0 AND `status` = 1;
	</select>
</mapper>